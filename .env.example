# Public runtime config only. Nothing in this project needs a secret yet —
# Firebase (Auth + Realtime Database) config will be added in a later
# assignment once accounts/favorites/checkout need a real backend. When
# that happens: real values go in .env.local (gitignored), and a matching
# placeholder entry gets added here so the required variables stay documented.

NEXT_PUBLIC_SITE_NAME=ShopFront
NEXT_PUBLIC_API_BASE_URL=https://fakestoreapi.com
# --- Secrets: real values .env.local e, kokhono ekhane na. ---
AI_GATEWAY_API_KEY=